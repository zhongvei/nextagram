import React from 'react';
import Loader from '';

const Loading = () => {
    return (
        <div>
            <p>hrllo</p>
            <image src={Loader}></image>
        </div>
    );
};

export default Loading;